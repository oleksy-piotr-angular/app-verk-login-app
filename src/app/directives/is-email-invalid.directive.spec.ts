import { IsEmailInvalidDirective } from './is-email-invalid.directive';

describe('IsEmailValidDirective', () => {
  it('should create an instance', () => {
    const directive = new IsEmailInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
