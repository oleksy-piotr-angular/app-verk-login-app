import { IsEmailValidDirective } from './is-email-invalid.directive';

describe('IsEmailValidDirective', () => {
  it('should create an instance', () => {
    const directive = new IsEmailValidDirective();
    expect(directive).toBeTruthy();
  });
});
